import { ArrowRight } from "lucide-react";
import { Button } from "@/components/ui/button";

export function CTA({ title = "Ready to Build Your Real Estate Empire?", text = "Join 10,000+ investors who are using Mr. Props to calculate risks, automate management, and maximize returns.", primaryButton = { label: "Get Started Free", href: "https://app.mrprops.io/register" }, secondaryButton = { label: "Talk to Sales", href: "/company/contact" } }: { title?: string; text?: string; primaryButton?: { label: string; href?: string }; secondaryButton?: { label: string; href?: string } }) {
  return <section className="py-24 bg-background relative overflow-hidden"><div className="absolute inset-0 bg-primary/5 -skew-y-3 transform origin-top-left scale-110 shadow-[0_0_40px_rgba(124,58,237,0.3)] border-b border-primary/10" /><div className="container mx-auto px-4 relative z-10 text-center"><h2 className="font-display text-4xl md:text-6xl font-bold mb-6 tracking-tight max-w-3xl mx-auto">{title}</h2><p className="text-xl text-muted-foreground max-w-2xl mx-auto mb-10">{text}</p><div className="flex flex-col sm:flex-row items-center justify-center gap-4"><Button size="lg" className="h-14 px-8 text-lg rounded-full bg-primary hover:bg-primary/90 shadow-xl shadow-primary/30 w-full sm:w-auto" asChild><a href={primaryButton.href || "https://app.mrprops.io/register"}>{primaryButton.label} <ArrowRight className="ml-2 h-5 w-5" /></a></Button><Button size="lg" variant="outline" className="h-14 px-8 text-lg rounded-full border-border bg-background hover:bg-muted w-full sm:w-auto" asChild><a href={secondaryButton.href || "/company/contact"}>{secondaryButton.label}</a></Button></div><p className="mt-6 text-sm text-muted-foreground">No credit card required. Cancel anytime.</p></div></section>;
}
