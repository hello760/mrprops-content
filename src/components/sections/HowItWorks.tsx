import { Bot, LayoutDashboard, LineChart } from "lucide-react";

export function HowItWorks() { const steps = [{ icon: LayoutDashboard, title: "Sync and Centralize", desc: "Connect your Airbnb, Booking.com, and VRBO listings to the Unified Inbox and Visual Calendar in seconds." }, { icon: Bot, title: "Automate with AI", desc: "Upload property FAQs to the Knowledge Base so the AI Concierge can handle guest queries 24/7." }, { icon: LineChart, title: "Optimize and Scale", desc: "Use real-time occupancy fractions and revenue analytics to track performance and grow your empire." }]; return <section className="py-24 bg-background"><div className="container mx-auto px-4"><div className="text-center max-w-3xl mx-auto mb-16"><h2 className="font-display text-3xl md:text-5xl font-bold mb-4">How It Works</h2><p className="text-muted-foreground text-lg">From acquisition to automation, we provide the blueprint for your property empire.</p></div><div className="grid md:grid-cols-3 gap-8 relative"><div className="hidden md:block absolute top-12 left-[16%] right-[16%] h-0.5 bg-gradient-to-r from-primary/0 via-primary/30 to-primary/0 border-t border-dashed border-primary/50 -z-10" />{steps.map((step, i) => <div key={i} className="flex flex-col items-center text-center group"><div className="w-24 h-24 rounded-full bg-secondary/50 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300 border border-border group-hover:border-primary/50 group-hover:bg-primary/5 shadow-lg group-hover:shadow-primary/20"><step.icon className="w-10 h-10 text-primary" /></div><h3 className="font-display text-xl font-bold mb-3">{step.title}</h3><p className="text-muted-foreground leading-relaxed max-w-xs">{step.desc}</p></div>)}</div></div></section>; }
