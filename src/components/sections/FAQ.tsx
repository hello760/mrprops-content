import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";

const defaultFaqs = [{ question: "How does the Unified Inbox work?", answer: "It connects directly to Airbnb, Booking.com, VRBO, and WhatsApp." }, { question: "Can the AI Concierge handle complex guest requests?", answer: "Yes. It uses listing context and prior conversations to answer common guest questions." }];
export function FAQ({ title = "Frequently Asked Questions", description = "Everything you need to know about scaling your portfolio with Mr. Props.", items = defaultFaqs }: { title?: string; description?: string; items?: Array<{ question: string; answer: string }> }) {
  return <section className="py-24 bg-secondary/20"><div className="container mx-auto px-4 max-w-3xl"><div className="text-center mb-16"><h2 className="font-display text-3xl md:text-5xl font-bold mb-4">{title}</h2><p className="text-muted-foreground text-lg">{description}</p></div><Accordion type="single" collapsible className="w-full space-y-4">{items.map((faq, i) => <AccordionItem key={i} value={`item-${i}`} className="border border-border bg-card rounded-xl px-6"><AccordionTrigger className="font-display font-semibold text-lg hover:text-primary hover:no-underline py-6">{faq.question}</AccordionTrigger><AccordionContent className="text-muted-foreground text-base pb-6 leading-relaxed">{faq.answer}</AccordionContent></AccordionItem>)}</Accordion></div></section>;
}
