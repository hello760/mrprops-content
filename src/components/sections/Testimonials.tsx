import { Star } from "lucide-react";

export function Testimonials() { const reviews = [{ name: "Sarah Jenkins", role: "Superhost (15 Units)", text: "Mr. Props changed the game for me. The renovation calculator alone saved me $20k on my last flip.", rating: 5 }, { name: "Mike Chen", role: "Property Investor", text: "Finally, a tool that looks good AND works.", rating: 5 }, { name: "Jessica Alverez", role: "Airbnb Arbitrage Pro", text: "I scaled from 1 to 10 units in 6 months using the insights from the blog and the calculator tools.", rating: 5 }]; return <section className="py-24 bg-background border-y border-border"><div className="container mx-auto px-4"><div className="text-center mb-16"><h2 className="font-display text-3xl md:text-5xl font-bold mb-4">Community Love</h2><p className="text-muted-foreground text-lg">Join thousands of happy hosts scaling their business.</p></div><div className="grid md:grid-cols-3 gap-8">{reviews.map((review, i) => <div key={i} className="bg-card border border-border p-8 rounded-2xl shadow-sm hover:shadow-xl transition-all duration-300"><div className="flex gap-1 mb-4">{[...Array(review.rating)].map((_, j) => <Star key={j} className="h-4 w-4 fill-accent text-accent" />)}</div><p className="text-lg font-medium leading-relaxed mb-6">&quot;{review.text}&quot;</p><div><div className="font-bold text-sm">{review.name}</div><div className="text-xs text-muted-foreground">{review.role}</div></div></div>)}</div></div></section>; }
