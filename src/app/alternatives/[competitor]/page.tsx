import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { FAQAndCTA, ProseBody } from "@/components/content/PageBits"; import { fetchAlternatives } from "@/lib/content-pages"; import { buildMetadata } from "@/lib/metadata"; export const revalidate = 3600; export async function generateStaticParams() { const pages = await fetchAlternatives(); return pages.map((page) => ({ competitor: page.slug })); } export async function generateMetadata({ params }: { params: Promise<{ competitor: string }> }): Promise<Metadata> { const { competitor } = await params; const page = (await fetchAlternatives()).find((item) => item.slug === competitor); return buildMetadata(page?.seoTitle || 'Alternatives', page?.seoDescription || 'Alternative page.', `/alternatives/${competitor}`); } export default async function AlternativePage({ params }: { params: Promise<{ competitor: string }> }) { const { competitor } = await params; const page = (await fetchAlternatives()).find((item) => item.slug === competitor); if (!page) notFound(); return <div className="min-h-screen bg-background"><div className="relative pt-24 pb-20 bg-secondary/10 border-b border-border overflow-hidden"><div className="container mx-auto px-4 text-center max-w-6xl relative z-10"><div className="inline-flex items-center gap-2 bg-red-100 text-red-600 px-4 py-1.5 rounded-full font-bold text-sm mb-8">{page.heroBadge || 'Stop Overpaying'}</div><h1 className="font-display text-5xl md:text-7xl font-bold mb-6 tracking-tight">{page.heroTitle || page.title}</h1><p className="text-xl md:text-2xl text-muted-foreground mb-10 max-w-3xl mx-auto leading-relaxed">{page.heroDescription || page.excerpt}</p></div></div><section className="container mx-auto px-4 max-w-screen-xl py-20"><ProseBody blocks={page.body} /></section><FAQAndCTA faqs={page.faqs} faqTitle={page.faqTitle} ctaTitle={page.ctaTitle} ctaText={page.ctaText} ctaPrimaryButton={page.ctaPrimaryButton} ctaSecondaryButton={page.ctaSecondaryButton} /></div>; }
