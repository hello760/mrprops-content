import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { FAQAndCTA, ProseBody } from "@/components/content/PageBits"; import { fetchComparisons } from "@/lib/content-pages"; import { buildMetadata } from "@/lib/metadata"; export const revalidate = 3600; export async function generateStaticParams() { const pages = await fetchComparisons(); return pages.map((page) => ({ slug: page.slug })); } export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const page = (await fetchComparisons()).find((item) => item.slug === slug); return buildMetadata(page?.seoTitle || 'Compare', page?.seoDescription || 'Comparison page.', `/compare/${slug}`); } export default async function ComparePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const page = (await fetchComparisons()).find((item) => item.slug === slug); if (!page) notFound(); return <div className="min-h-screen bg-background"><section className="px-4 pb-12 pt-16 md:pt-20"><div className="mx-auto max-w-6xl"><div className="mx-auto max-w-3xl text-center"><div className="mb-4 inline-flex items-center rounded-full border border-primary/15 bg-primary/5 px-4 py-1.5 text-xs font-semibold uppercase tracking-[0.18em] text-primary md:text-sm">{page.heroBadge || '2026 Software Showdown'}</div><h1 className="font-display text-4xl font-bold tracking-tight text-foreground md:text-6xl">{page.heroTitle || page.title}</h1><p className="mx-auto mt-4 max-w-2xl text-base leading-7 text-muted-foreground md:text-lg">{page.heroDescription || page.excerpt}</p></div></div></section><section className="container mx-auto px-4 max-w-screen-xl pb-20"><ProseBody blocks={page.body} /></section><FAQAndCTA faqs={page.faqs} faqTitle={page.faqTitle} ctaTitle={page.ctaTitle} ctaText={page.ctaText} ctaPrimaryButton={page.ctaPrimaryButton} ctaSecondaryButton={page.ctaSecondaryButton} /></div>; }
